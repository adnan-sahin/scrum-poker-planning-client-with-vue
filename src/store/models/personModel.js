export default class PersonModel {
  constructor(id, name, type, story_id, plan_id, point) {
    this.id = id;
    this.name = name;
    this.type = type;
    this.story_id = story_id;
    this.plan_id = plan_id;
    this.point = point;
  }
}
